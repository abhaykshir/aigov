# Auto-detect text files and normalize line endings to LF in the repository.
# Working trees on Windows still get CRLF on checkout; commits land as LF.
* text=auto eol=lf

# Binary files — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.gz binary
*.tar binary
