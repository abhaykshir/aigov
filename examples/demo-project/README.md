# aigov demo project

This simulates a security team's AI sprawl — threat intelligence, customer chatbot, FinOps analysis, and internal tooling. Run `aigov scan examples/demo-project --classify --with-risk` or `aigov graph examples/demo-project --out-file graph.html` to see aigov in action.
